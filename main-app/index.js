import React from 'react';
import ReactDOM from 'react-dom';
import App from './src/App';

const MicroFE = () => (
  <div>
    <App />
  </div>
);

ReactDOM.render(<MicroFE />, document.getElementById('app'));
